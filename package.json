{
  "name": "insurance-wizard-server",
  "version": "1.0.0",
  "description": "Insurance Wizard - Node.js Express Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "react-scripts build",
    "install-all": "npm install && cd client && npm install",
    "build-client": "cd client && npm run build && cd ..",
    "deploy": "npm run build-client && npm start"
  },
  "keywords": [
    "insurance",
    "wizard",
    "call-center",
    "playbook"
  ],
  "author": "Your Company",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
